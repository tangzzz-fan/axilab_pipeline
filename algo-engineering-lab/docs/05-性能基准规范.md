# 05 · 性能基准规范

版本：0.1.0 · 日期：2026-07-28 · 作者：lab · 状态：草稿

> 面试口述的每个性能数字必须可复现。精度与性能分离：优化不得改变 golden 行为。

---

## 1. 基准环境记录格式（五项缺一无效）

| 字段 | 示例 |
| :--- | :--- |
| 机型 | MacBook Pro 14-inch 2023 |
| 芯片 | Apple M2 Pro |
| OS / iOS | macOS 15.x / iOS 18.x |
| 编译配置 | Release，`-O`，未附加 Sanitizer |
| 电量与发热 | 电源适配器；机身温热可接受 / 过热降频则作废 |

记录时追加：日期、`git` commit hash、Xcode/Swift 版本。

---

## 2. 指标定义

| 指标 | 定义 | 测量方法 |
| :--- | :--- | :--- |
| 耗时 p50 / p95 | 同一输入重复 N≥30 次的分位数 | XCTest `measure` 或 BenchmarkTests 自写循环；丢掉预热 3 次 |
| 内存峰值 | 任务区间内 dirty 峰值 | Instruments Allocations 或 `os_proc` 采样（注明） |
| 单位吞吐 | 样本/秒 或 窗口/秒 | 总样本数 / 墙钟时间 |

禁止只报单次耗时。

---

## 3. 记录格式（追加式，禁止覆盖历史）

在 `docs/06-实验复盘/` 或本目录附属 `bench-log.md` 追加：

| 日期 | commit | case | 实现 | p50 (ms) | p95 (ms) | 加速比 | 环境摘要 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |

---

## 4. 防退化门槛

同环境回归：**p50 恶化 >20%** 必须在提交说明中解释，否则视为事故。

---

## 5. 精度与性能分离

- ParityTests：只断言精度，CI 必过。
- BenchmarkTests：只记录性能，**不**阻塞 CI（可本地/手动跑）。
- 优化版与 naive 版必须过**同一套** golden；「优化不许改变行为」。
