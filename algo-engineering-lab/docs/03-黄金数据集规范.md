# 03 · 黄金数据集规范

版本：0.2.0 · 日期：2026-07-28 · 作者：lab · 状态：生效

> golden 是真值。只允许由 Python 原型生成；禁止从 C/Swift 反向「修正」golden。

---

## 1. 生成流程

1. 入口：`uv run python -m python.generate_golden`（各 case 子命令或模块，随实现补齐）。
2. **固定随机种子**（默认 `seed=42`），写入每条记录的 `meta.seed`。
3. Python 依赖以 `uv.lock` 锁定；升级依赖后必须重生 golden 并独立提交说明。
4. 生成物写入 `golden/<case_id>/*.json`。

---

## 2. 数据格式约定（JSON）

每条记录至少包含：

```json
{
  "id": "hrv_td_001",
  "case": "hrv_time_domain",
  "category": "normal | boundary | pathological",
  "pathology": "none | missing_beat | ectopic | empty | single | extreme_noise | null",
  "input": { "rr_ms": [800.0, 810.0], "non_finite_mode": null },
  "expected": { "sdnn_ms": 0.0, "rmssd_ms": 0.0, "pnn50": 0.0 },
  "snapshots": {},
  "meta": {
    "seed": 42,
    "generator": "python/hrv_prototype.py",
    "source": "synthetic | physionet:<dataset>",
    "license_note": "synthetic internal"
  }
}
```

- `snapshots`：频域等长链路 case 使用；时域可为 `{}`。
- 公开数据（PhysioNet 等）必须写 `source` 与 `license_note`。

---

## 3. 覆盖矩阵

| Case | 记录数 | 正常 | 边界 | 病态 | 备注 |
| :--- | ---: | ---: | ---: | ---: | :--- |
| `hrv_time_domain` | 51 | 22 | 3 | 26 | 病态含 non_finite / missing_beat / ectopic / extreme_noise |
| `fir_ppg` | 52 | 约 30% | 覆盖 | ≥40% | 输出与系数双重对齐 |
| `hrv_freq_domain` | 53 | 22 | 3 | 28 | 含逐环快照（resampled→psd） |
| `streaming_fir` | 3 | 1 | 0 | 2 | 连续流 / 丢包 zero_fill / 空输入 |
| `hrv_artifact_correction` | 56 | 22 | 2 | 32 | 含 `artifact_mask` 与 `rr_corrected` |
| `ota_dfu_state_machine` | 30 | 13 | 0 | 17 | 终态 + 漏斗 + 进度对齐 |
| `multi_channel_sync` | 33 | 12 | 0 | 21 | 漂移/丢包/乱序，`timeline+mask` 对齐 |
| `sleep_staging_postprocess` | 33 | 12 | 0 | 21 | `raw/smoothed/metrics` 对齐 |
| `coreml_quant` | 1 | 报告型 | — | 报告型 | 含 `verification_vectors` 固定输入输出 |
| `coreml_drift_monitoring` | 1 | 报告型 | — | 报告型 | stable vs shifted 告警语义断言 |

> 说明：CoreML 两个 case 为“报告型 golden”，不按逐样本 JSON 组织；通过固定输入向量与语义阈值断言保证可回归。  
> 逐样本 case 仍遵循建议：每算法建议 ≥50 条，**病态占比 ≥40%**（`streaming_fir` 为特例演示集）。

---

## 4. 冻结与版本化

- golden 变更必须**独立提交**，说明原因；不得与实现改动混 commit（说明书工程纪律）。
- 禁止用 C/Swift 输出覆盖 expected。
- golden 变更后，`docs/05` 历史性能数字作废重测。

---

## 5. 复现 DoD

删除 `golden/` 后按脚本重跑，产出应与版本库**逐字节一致**（或 JSON 规范排序后一致——若采用排序，须在本规范冻结「canonical JSON」规则）。当前约定：生成器写出 **sorted keys + 固定浮点格式**（`%.9g`），保证稳定。
